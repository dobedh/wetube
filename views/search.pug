extends layouts/main
include mixins/videoBlock

block content
    .search__header
        h3 Search for : #{searchingBy}
    .search__videos
        // To-do : show/filter the exact videos the user searched
        each item in videos
            +videoBlock( {
                id : item.id,
                title: item.title,
                views: item.description,
                videoFile: item.fileUrl
            })
