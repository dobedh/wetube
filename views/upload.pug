extends layouts/main

block content
    .form-container
        form.upload(action=`/videos${routes.upload}`, method="post")
            label(for="upload") Video Upload
            input(type="file", id="upload", name="upload", required=true)
            input(type="text", placeholder="Title", name="title", required=true)
            textarea(name="textarea", cols="30", rows="5", placeholder="description", required=true)
            input(type="submit", value="Upload Video")
        a.fomr-contatiner__link(href=routes.changePassword) Change Password